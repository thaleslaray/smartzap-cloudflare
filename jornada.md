# Jornadas do usuário — SmartZap

Este é o catálogo canônico e vivo das jornadas do produto. Ele descreve o que precisa existir e ser testado. Resultados detalhados de cada rodada pertencem a `Auditoria.md`.

Última consolidação: 01/08/2026
Histórico importado de: `docs/auditoria-jornadas.md`

## Regras de manutenção

- Toda nova função, rota, integração, estado ou variação deve entrar aqui na mesma alteração de código.
- Toda lacuna descoberta deve ser adicionada imediatamente como `não testada`.
- Uma jornada só pode ficar `aprovada` quando houver evidência compatível registrada em `Auditoria.md` ou no histórico legado.
- Alterações funcionais reabrem as jornadas afetadas como `corrigida — reteste pendente` até novo teste.
- Os IDs são permanentes. Jornadas removidas ficam `descontinuada`; não reutilizar o ID.

## Catálogo

| ID | Área | Jornada e variações mínimas | Entrada principal | Estado atual |
| --- | --- | --- | --- | --- |
| AUTH-01 | Autenticação | Login correto/incorreto, persistência, expiração e logout | `/login` | aprovada |
| DASH-01 | Dashboard | Carregamento, períodos, atalhos, vazio e erro | `/` | aprovada |
| DASH-02 | Dashboard | Resumo operacional simples e responsivo, sem a composição premium/editorial: prioriza o diagnóstico do momento (atividade, estabilidade ou falhas a revisar), identifica campanha e motivo da falha recente com acesso direto ao detalhe, seguido por métricas distintas de envio, entrega, leitura, campanhas e falhas; gráfico de volume e campanhas recentes se reorganizam por breakpoint sem overflow. Preserva estados sem dados, erro, recuperação, período e ação de campanha rápida. | `/` | aprovada |
| DES-01 | Direção visual | Mockup navegável da direção Apple-inspired premium: shell responsivo, camadas de superfície, métricas, gráfico, atividade e campanhas; ações são demonstrativas e não alteram dados | `/design-preview` | em teste |
| DES-02 | Direção visual | Aplicar a linguagem visual clássica do SmartZap nas rotas autenticadas: superfícies escuras diretas, navegação lateral utilitária, ações verdes retangulares, cartões sem efeitos editoriais e estados responsivos; preservar as jornadas operacionais e os estados responsivos. | aplicação autenticada | corrigida — reteste pendente |
| DES-03 | Direção visual | Refinar a aplicação premium após inspeção visual: seleção de navegação em vidro escuro com indicador mint, superfícies em camadas, raios e sombras editoriais, hierarquia tipográfica, gráfico com acento mint e composição de métricas mais compacta; CTAs mint usam texto escuro com contraste legível; manter o acento verde reservado às ações e estados | aplicação autenticada | corrigida — reteste pendente |
| NAV-01 | Navegação global | Menu e breadcrumb; funções contextuais de público não ocupam item próprio no menu global; Tutoriais abre ajuda inline; tema persiste; Alertas refletem a saúde real, oferecem rotas de correção quando há pendência e confirmam explicitamente quando a operação está pronta; o antigo botão de Modo Desenvolvedor foi removido porque não acionava ferramenta alguma; rota inexistente ou legado removido apresenta destino explícito, nunca uma casca vazia | aplicação | aprovada |
| ONB-01 | Onboarding | Primeira instalação exibe checklist idempotente derivado da saúde real de credenciais, Meta, webhook e templates no Dashboard e em Configurações; não obstrui Inbox, campanhas ou demais superfícies operacionais; permite retomar, dispensar e navegar para a correção | entrada autenticada, `/` e `/settings` | aprovada |
| PWA-01 | Aplicativo instalável | Manifesto, instalação, atualização e service worker; banner aparece somente quando o navegador oferece instalação ou há atualização | aplicação | corrigida — reteste pendente |
| CNT-01 | Contatos | Lista, busca, ordenação, filtros expansíveis, paginação, perfil e estado vazio após exclusão, com paridade funcional e visual ao original. A confirmação de exclusão individual mantém ícone e rótulo da ação destrutiva centralizados como um único grupo em desktop e mobile. A barra de filtros distribui botão, status e tag em uma grade fluida na largura de 360 px e preserva as larguras compactas em telas maiores, sem overflow. Loading, vazio, erro e retentativa têm E2E nos três motores; comparação visual usa fixture equivalente e sete viewports. Ações de modal inexistentes no legado mobile são cobertas funcionalmente no migrado e registradas como limitação da referência. | `/contacts` | corrigida — reteste pendente |
| CNT-02 | Contatos | Criar manual não infere opt-in: sem declaração explícita, o contato nasce como `unknown` e permanece inelegível para disparos até consentimento válido. Criar, editar, nome, telefone, e-mail, tags, campos personalizados, opt-out e supressão; modais e estados com paridade ao original. | `/contacts` | aprovada |
| CNT-03 | Contatos | Importar CSV, validar, tratar duplicatas/falhas e exportar; sem declaração explícita, a importação mantém contatos como `unknown`, sem elegibilidade para disparos. Modais e feedbacks têm paridade ao original. | `/contacts` | aprovada |
| CNT-04 | Contatos | Operações em massa e consistência do perfil. A ficha avançada de memória/histórico foi descontinuada para preservar o fluxo do SmartZap original; clicar no contato abre a edição compacta. | `/contacts` (modais) | aprovada |
| SEG-01 | Públicos salvos | Na etapa Público da campanha, combinar tags, DDI e DDD, dar nome e salvar o público para reutilização; ao salvar, o público reaplica exatamente seus critérios. Um público selecionado pode ser excluído inline, com confirmação explícita no mesmo bloco. A rota legada `/segments` não é item de navegação global. | `/campaigns/new` | corrigida — reteste pendente |
| SEG-02 | Segmentação geográfica | Selecionar e buscar todos os países/territórios do catálogo de DDI, as 27 UFs e os 67 DDDs brasileiros; aplicar alcance (OR) e precisão (AND), preservar a regra especial do DDD 61 compartilhado por DF/GO e informar que países que compartilham DDI não são distinguíveis apenas pelo prefixo. A seleção de UF/DDD usa uma grade regular de três colunas, com 27 UFs em nove linhas completas de chips idênticos; fica desabilitada até Brasil ser selecionado. | `/campaigns/new` | corrigida — reteste pendente |
| TMP-01 | Templates | Listar, buscar, filtrar, status e prévia; sincronizar manualmente e automaticamente ao salvar Phone ID/WABA válidos. Os filtros de categoria e status quebram linha responsivamente, sem barras de rolagem internas expostas ou categorias cortadas. Falha da sincronização automática preserva a configuração e a cópia local, informa o operador e permite nova tentativa manual. | `/templates` e `/settings` | aprovada |
| TMP-02 | Templates | Criar rascunho com navegação no rodapé do formulário, prévia viva sem ação duplicada, inserção de variável na posição do cursor, idiomas, categorias e botões; no celular, usar a ação flutuante de prévia e manter os três passos legíveis dentro do viewport, sem rótulo cortado ou overflow interno. Antes de avançar ou publicar, aplicar as regras oficiais da Meta: corpo com até 1.024 caracteres, exemplo para cada variável, parâmetros posicionais iniciando em `{{1}}` e sem lacunas, e proibição de variável no início ou fim da mensagem. A regra qualitativa de excesso de variáveis deve ser explicada sem inventar proporção numérica. | `/templates/drafts/new` | aprovada |
| TMP-03 | Templates | Editar sem apagar componentes ou botões avançados não representados pela interface simplificada; validar, publicar na Meta, acompanhar status e excluir. | `/templates/drafts/:id` | corrigida — reteste pendente |
| TMP-05 | Templates | Paridade completa do editor rico original: formato de parâmetros, cabeçalho de texto/mídia/localização, exemplos, formatação, LTO, carrossel e botões COPY_CODE/OTP/FLOW. | `/templates/drafts/new` e `/templates/drafts/:id` | não testada |
| TMP-04 | Templates | Clonar template, resolver nomes duplicados e preservar componentes/idioma | `/templates` | aprovada |
| PRJ-01 | Projetos | Criar projeto, configurar campos, revisar e gerar ativos | `/templates/projects/new` | aprovada |
| PRJ-02 | Projetos | Reabrir, editar, persistir, retomar e excluir projeto | `/templates/projects/:id` | aprovada |
| CMP-01 | Campanhas | Lista, busca, filtros, pastas, métricas e estados vazios. Status, pasta e tags usam popovers internos uniformes, sem selects nativos; os menus não devem vazar nem cortar conteúdo na largura reduzida. Uma campanha tecnicamente concluída, mas com falhas, aparece como `Concluída com falhas` em alerta, nunca como sucesso pleno; o filtro `Falhou` inclui tanto o estado técnico `failed` quanto qualquer campanha com falhas registradas. A lista mostra somente Entrega Meta, deixando explícito que é confirmação por webhook; o progresso de envio (aceite/erro do lote) fica no detalhe e Eventos operacionais, evitando coluna vazia/redundante. | `/campaigns` | aprovada |
| CMP-02 | Campanhas | Configuração, template e variáveis no contrato do original: um campo de valor por `{{n}}` e botão `{}` para inserir nome, telefone, e-mail ou campo personalizado; fallback e prévia única na lateral; bloquear avanço com variável sem mapeamento. Quando um contato não tiver valor para a fonte escolhida, a interface identifica a variável e a área afetada e orienta escolher outra fonte ou preencher fallback, sem expor chaves internas como `body.3`. | `/campaigns/new` | aprovada |
| CMP-03 | Campanhas | Público por todos, contatos, teste e segmento; opt-out, supressão e duplicidade. A segmentação rápida inclui o catálogo completo de países/territórios por DDI e as 27 UFs/67 DDDs do Brasil, com busca e sem confundir países que compartilham um código internacional. Ao selecionar `Teste`, usa automaticamente o único destinatário configurado, prepara seu opt-in/evidência e renderiza a prévia real sem pedir nova seleção. | `/campaigns/new` | aprovada |
| CMP-04 | Campanhas | Validação, estimativa, agenda, fuso, rascunho e disparo autorizado; orientar correção conforme o motivo do contato ignorado | `/campaigns/new` | aprovada |
| CMP-05 | Campanhas | Detalhe, lotes, métricas, status, falha, pausa e cancelamento. Uma campanha concluída com falhas deve manter o estado técnico, mas exibir `Concluída com falhas` em alerta e apontar a falha nos logs; dados técnicos de throughput e duração ficam em Eventos operacionais/Performance, não no fluxo principal da campanha | `/campaigns/:id` | aprovada |
| CMP-06 | Campanhas | Pastas, cores, tags, duplicação, relatório CSV e reenvio de ignorados. Ao clonar, preserva template, mapeamento, pasta, tags e definição de público; recalcula a contagem elegível atual sem copiar envios/histórico. Quando não houver tags, o filtro e a ação por campanha oferecem criação direta em Organizar Campanhas na aba Tags. Em cada campanha, mover para pasta usa menu próprio acessível, com “Sem pasta”, indicação da pasta selecionada e sem menu nativo do navegador; a seleção de tag apresenta o novo estado imediatamente, preserva-o durante a atualização e reverte em caso de erro; exclusão individual e em lote usam modal interno com confirmação explícita; uma seleção com campanha em envio ou pausada é bloqueada integralmente. | `/campaigns` e `/campaigns/:id` | corrigida — reteste pendente |
| PRC-01 | Preços | Selecionar rate card por país, categoria, moeda e vigência; em Configurações, importar automaticamente as tarifas BRL oficiais da Meta/WhatsApp, validá-las e ativá-las, informando claramente se houve atualização ou se a tabela já estava vigente; upload CSV somente como contingência | API de pricing/campanha e `/settings` | corrigida — reteste pendente |
| PRC-02 | Preços | Calcular tiers, estimativa por mensagem e custo do lote | API de pricing/campanha | aprovada |
| PRC-03 | Preços | Converter USD/BRL e usar a última cotação válida como fallback | API de câmbio/campanha | aprovada |
| PRC-04 | Preços | Registrar snapshot estimado e confirmação posterior da Meta; no detalhe, exibir ambos em faixa compacta, sem competir com métricas e logs operacionais. | detalhe da campanha | corrigida — reteste pendente |
| PRC-05 | Preços | Exibir indisponibilidade sem inventar taxa ou custo | wizard/detalhe | aprovada |
| INB-01 | Inbox | Lista, busca, filtros, conversa, não lidas e atualização em tempo real | `/inbox` | aprovada |
| INB-02 | Inbox | Enviar/receber texto, template e mídia; status, janela Meta e falhas | `/inbox/:id` | aprovada |
| INB-03 | Inbox | Respostas rápidas, labels, notas, handoff, atendente e histórico | `/inbox/:id` | aprovada |
| ATD-01 | Atendimento | Fila, atribuição, disponibilidade e conversa por link direto | `/atendimento` | aprovada |
| ATD-02 | Atendimento | Criar, editar, ativar/desativar atendentes e permissões | `/settings/attendants` | aprovada |
| KNO-01 | Conhecimento | Criar, editar, upload, indexar, buscar, reindexar e excluir | `/knowledge` | aprovada |
| AI-01 | IA | Configuração de um provedor, limites, indisponibilidade e erro; o timeout do provider cobre respostas fundamentadas lentas sem deixar chamadas penduradas | `/settings/ai` | aprovada |
| AI-02 | IA | Criar agente, instruções, RAG, ativar/desativar, testar e excluir; o prompt fundamentado impõe opt-in/consentimento para disparos e não permite inferir consentimento por importação; erro transitório ou saída vazia admite uma segunda tentativa finita, sem repetir timeout | `/settings/ai/agents` | aprovada |
| AI-03 | IA | Sugestão na Inbox, contexto, memória, RAG, segurança e handoff; respostas fundamentadas aguardam o limite operacional de 30 segundos, repetem apenas falha transitória/saída vazia uma vez, rejeitam reciclagem semântica da última resposta e falham fechadas depois disso | `/inbox/:id` | aprovada |
| AI-04 | IA | Prompt injection, isolamento entre contatos, fonte ausente, timeout e falha segura do provedor; importação ou ausência de opt-out nunca vira consentimento, disparo em massa exige evidência e falhas do provider ficam identificadas por código sanitizado no simulador | Inbox, agentes e conhecimento | aprovada |
| AI-05 | IA comercial | Configurar identidade, prompt, regras de qualificação, FAQ operacional, RAG, consentimento, campanha, objeção comercial e handoff por preço/contrato; validar no canal real. O prompt fundamentado exige consentimento explícito e segmentação antes de orientar disparos; preço, prazo, proposta, pedido de humano e estados canônicos de entrega têm política determinística no envio automático | `/settings/ai/agents`, `/knowledge` e `/inbox/:id` | corrigida — reteste pendente |
| INB-04 | Inbox/IA | Configurar timeout humano, retenção, chave global de Atendimento IA e confirmar que desligar a chave impede a automação autônoma em conversas já habilitadas | `/inbox` e `/settings/ai/agents` | corrigida — reteste pendente |
| AI-06 | IA | Desativar um agente e confirmar que conversas já atribuídas deixam de gerar, aprovar e enviar respostas automáticas; reativar e confirmar retomada controlada | `/settings/ai/agents` e `/inbox/:id` | corrigida — reteste pendente |
| AI-07 | IA | Paridade entre saúde central, estado do agente, documentos indexados, metadados exibidos e prontidão real do provedor | `/settings/ai`, `/settings/ai/agents` e `/knowledge` | corrigida — reteste pendente |
| AI-08 | IA na Inbox | Cada controle do cabeçalho deve ter semântica própria: contexto/memória, modo/agente e menu de ações; a cópia deve distinguir rascunho assistivo de automação autônoma | `/inbox/:id` | aprovada |
| WHK-01 | Teste de IA via WAHooks | Usar uma conexão WAHooks autorizada apenas como número de teste: enviar texto ao número oficial Meta, receber o inbound no webhook Meta, acionar IA/RAG na conversa e devolver a resposta pela API oficial ao número WAHooks. Não inclui callback ou adaptador WAHooks dentro do SmartZap. | WAHooks → Meta oficial → webhook Meta → Inbox/IA → API Meta → WAHooks | aprovada |
| WHK-02 | Conversa multi-turno via WAHooks | Simular vários turnos no mesmo chat, mantendo contexto, confirmando e reformulando a resposta, acionando handoff seguro quando a pergunta fica sem fonte suficiente e retomando a IA sem criar loop. | WAHooks → Meta oficial → webhook Meta → Inbox/IA ↔ WAHooks | em teste |
| WHK-03 | Simulação comercial real via WAHooks | Prospecto começa sem histórico, pergunta sobre produto, API oficial, 2.000 contatos, opt-in, templates, audiência e entrega; a IA qualifica sem inventar preço e transfere intenção de contratação para humano | WAHooks → Meta oficial → Inbox/IA comercial | corrigida — reteste pendente |
| MINI-01 | MiniApps | Listar, buscar, filtrar, atualizar e abrir | `/flows/builder` | aprovada |
| MINI-02 | MiniApps | Criar do zero, validar nome, editar, salvar e reabrir. Todo MiniApp recém-criado ou reaberto inicia no passo “Começar”, preservando as três escolhas do original: começar do zero, usar modelo pronto e criar com IA. | `/flows/builder` e `/flows/builder/:id` | aprovada |
| MINI-03 | MiniApps | Criar e validar cada template estático disponível. Como no legado, “Criar por template” abre um modal controlado por estado local dentro de `/flows/builder`, sem alterar a URL; “Criar MiniApp” na aba MiniApps de Templates cria um fluxo vazio e abre diretamente o editor. Dentro do passo “Começar” de `/flows/builder/:id`, “Usar modelo pronto” também deve permitir selecionar e aplicar um template ao fluxo aberto. | `/flows/builder`, `/flows/builder/:id` e `/templates` | aprovada |
| MINI-04 | MiniApps | Criar com IA pela lista e pelo passo “Começar” do editor; validar prompt, cancelamento, loading, erro, estrutura gerada e persistência | `/flows/builder` e `/flows/builder/:id` | aprovada |
| MINI-05 | MiniApps | Telas, blocos, regras, ramificações, ordem, preview e persistência. A matriz obrigatória inclui Título, Subtítulo, Texto, Legenda, texto curto (texto/e-mail/telefone/número), texto longo, data, dropdown, escolha única, múltipla escolha e opt-in; deve comprovar criação, edição, reordenação, exclusão, required/opções, múltiplas telas, destino padrão, regra condicional, tela final e prévia interativa. Em “Finalizar”, o nome continua editável, o mapeamento do contato é revisável e a publicação permanece explícita. Preservar o acesso a “Ajustes avançados” do editor original para manutenção estrutural de telas e rotas, incluindo atualização das referências ao renomear uma Screen ID. A matriz de 14 tipos suportados, três telas, rota padrão, ramificação condicional, conclusão e prévia responsiva foi aprovada em Chromium, Firefox e WebKit; a jornada permanece em teste porque ainda falta o reteste integral de reordenação, exclusão, mapeamento final e ajustes avançados. | `/flows/builder/:id` | em teste |
| MINI-06 | Meta Flows | Gerar e validar Flow JSON para cada componente e combinação suportada, criar draft, validar/publicar no provedor, enviar, receber callback e tratar rejeição. A aprovação exige contrato local completo e validação real da Meta, explicitando quando a evidência for apenas local. A publicação real foi aceita pela Meta como `PUBLISHED` (Flow ID técnico `3495906073919692`, sem erros de validação) e o envio ao número de testes foi aceito (HTTP 200, status local `sent`); entrega, leitura, callback, conclusão da interação e rejeição continuam sem confirmação. | `/flows/builder/:id` | em teste |
| MINI-07 | Meta Flows | Flow dinâmico, criptografia, INIT, data_exchange, persistência e Google Calendar opcional | `/flows/builder/:id` | bloqueada |
| FORM-01 | Formulários | Criar, editar, campos, slug, publicar/inativar e excluir; menus de tag e tipo de campo usam componentes visuais consistentes, permitem criar tag no próprio editor e exibem a quantidade e o acesso direto às respostas. | `/forms` | corrigida — reteste pendente |
| FORM-02 | Formulários | Validar e enviar formulário público, persistindo nome, telefone e e-mail; exigir consentimento explícito para WhatsApp, registrar a evidência e criar ou atualizar o contato com status opt-in. Cobrir erro, confirmação e indisponibilidade. | `/f/:slug` | corrigida — reteste pendente |
| FORM-03 | Submissões | Listar, buscar, filtrar por origem, detalhar e exportar respostas de Forms públicos e MiniApps. Cada item deve identificar explicitamente a origem e o ativo que recebeu a resposta; no resumo, cada campo aparece em uma linha própria, sem concatenação por separadores; “Ver respostas” abre todos os campos em formato legível, sem JSON achatado ou truncado. O acesso pelo card ou cabeçalho de Forms abre as respostas já filtradas pelo formulário. | `/submissions` | corrigida — reteste pendente |
| SET-01 | Configurações | Carregar IDs reais como valores editáveis (não como placeholders), mascarar segredos, validar, salvar, limpar/desconectar e recarregar configurações; ao salvar Phone ID/WABA, sincronizar templates automaticamente. Se a Meta falhar depois da persistência, manter os IDs salvos e retornar sucesso com aviso de sincronização, sem falso erro 502. | `/settings` | aprovada |
| SET-02 | Meta | Diagnóstico de credenciais, conexão, webhook, templates e erros | `/settings/meta-diagnostics` | aprovada |
| SET-03 | Performance | Métricas, filtros, períodos, throughput, vazio e erro | `/settings/performance` | aprovada |
| SET-04 | Configurações | Contato de teste, retenção da Inbox, timeout humano e persistência operacional | `/settings` | aprovada |
| SET-05 | Configurações | Uso da infraestrutura: backlog real das Queues, envios confirmados no mês, invocações mensais do Worker, storage e linhas lidas/escritas no D1 via token de Analytics com menor privilégio. Falha de acesso deve exibir indisponibilidade, nunca zero estimado. | `/settings` | aprovada |
| META-01 | Meta | Receber, persistir e responder por BSUID/username quando o telefone estiver presente ou omitido; status sem `recipient_id` | webhooks, Inbox e envio | em teste |
| CAL-01 | Integrações | Configurar OAuth Google, conectar/desconectar, escolher calendário, agenda, slots e evento | `/settings` e MiniApp | bloqueada |
| WEB-01 | Webhook | Verificação, assinatura, entrada, fila, idempotência, replay e erro | `/webhook` | aprovada |
| WEB-02 | Webhook | Status sent, delivered, read e failed sem regressão ou duplicidade | `/webhook` | aprovada |
| WEB-03 | Webhook | Payload até 3 MB, eventos de conta/qualidade/throughput e capacidade de entrega | `/webhook` e Queue | aprovada |
| SEC-01 | Segurança | Autorização, IDs, XSS, uploads, injeção, rate limit e proteção de segredos | aplicação e APIs | aprovada |
| SEC-02 | Superfície descontinuada | Funcionalidades descontinuadas ou fora do escopo não podem manter UI morta nem APIs mutáveis montadas; Workflows responde `410 WORKFLOWS_RETIRED`, não consome Queue e preserva somente dados históricos | aplicação e APIs | aprovada |
| RSP-01 | Responsividade | Todas as rotas operacionais são auditadas em 360, 390, 620, 768, 1440 e 1920 px; telas críticas adotam mobile-first, toolbars quebram em largura reduzida, textos e controles não escapam dos próprios cards, Inbox alterna entre lista e conversa em tela cheia no mobile e tabelas permanecem em rolagem horizontal controlada quando necessário. A barra de filtros de Contatos mantém seus três controles dentro do viewport mínimo mesmo quando os filtros estão expandidos. | aplicação | corrigida — reteste pendente |
| A11Y-01 | Acessibilidade | Teclado, foco, labels, contraste, zoom, modais e mensagens de erro; animações de status não podem reduzir a opacidade nem o contraste do texto | aplicação | aprovada |
| ERR-01 | Resiliência | Loading, vazio, timeout, 4xx/5xx, recuperação e Error Boundary nas rotas críticas | aplicação | aprovada |
| OPS-01 | Operação | Health, filas, crons, reconciliação, retenção, limpeza e recuperação de falhas | Worker Cloudflare | aprovada |
| QA-01 | Controle autônomo de qualidade | Executar um controlador único que correlaciona versão, jornada, risco, testes, evidências e cleanup; oferecer gates `preflight`, unitário, contrato, E2E P0, matriz cross-browser, visual, IA, canário Meta e relatório consolidado; reprovar quando houver flake P0/P1, segredo, resíduo, erro interno do Worker ou evidência ausente. O controlador isola relatórios por navegador e reprova qualquer teste que só passe após retry; o eval de IA exige 100% em pass1, pass3, allAttempts e grounding factual; os grupos geral, Inbox, responsividade e WCAG usam processos independentes nos dois motores. O gate determinístico remoto passou sem flake; staging confirmou Worker, D1, health, autenticação, smoke, 84 sessões reais do Workers AI, cleanup e primeira prova do Cron; produção foi explicitamente pulada. | CLI/CI e ambientes local, preview e staging | aprovada |
| QA-02 | Isolamento de testes | Cada execução mutante recebe `run_id`, prefixo `AUTOQA`, fixtures, tag, journal e storage próprios, providers falsos por padrão e limpeza por IDs exatos; o gate local não chama Meta, Workers AI ou Google Calendar reais. App, inspetor, nome do Worker, Durable Objects e persistência são exclusivos por execução; concorrência entre o gate completo e a matriz cross-browser não pode gerar colisão nem erro interno. O cleanup também descobre órfãos e exige resíduo zero de agentes, documentos, campanhas, contatos e tags. | runtime de teste, D1/R2/Queues/DO locais e Playwright | aprovada |
| QA-03 | Canário Meta autônomo | Em staging, criar ou atualizar somente os quatro contatos autorizados, aplicar uma tag `AUTOQA` exclusiva por `run_id`, persistir o journal antes da mutação, respeitar allowlist, templates, janela e limites, correlacionar aceite, `wamid`, webhooks e timeline, validar inbound/IA/outbound e remover somente artefatos da própria execução; qualquer violação aciona stop-the-line. O CI troca o callback apenas entre os dois Workers canônicos, executa o canário e restaura produção em `always`, sem aceitar URL arbitrária; a proteção do callback está implementada e o transporte oficial está coberto, mas o ciclo real permanece pendente por janela/credencial WAHooks. | staging Cloudflare ↔ Meta Cloud API ↔ números autorizados | corrigida — reteste pendente |
| QA-04 | Laboratório e promoção segura | Executar 28 cenários de IA em três tentativas, com graders determinísticos e juiz calibrado; pass1, pass3 e allAttempts exigem 100%, sem esconder flake por limiar. Promover preview → staging → canário → piloto somente com gates verdes, observabilidade e rollback ensaiado em até 10 minutos. Preview e staging usam credenciais próprias rotacionadas; preview mantém integrações externas inertes; toda mutação de staging é serializada. Preview, staging, 84 evals reais e rollback foram comprovados; canário com callback completo, calibração humana do juiz e soak de 14 dias continuam pendentes. | CI, staging, Workers AI, Meta e piloto | em teste |
| QA-05 | Monitoramento autônomo do soak | A cada cinco minutos, um Worker observador isolado verifica pela entrada pública o health e o shell de staging e produção, aplica timeout e retry finitos, registra o resultado sanitizado com expiração, expõe somente o último estado não secreto e tolera a entrega pelo menos uma vez do Cron sem produzir efeitos no SmartZap. O monitor explicita a compatibilidade de fetch público necessária para consultar Workers da mesma zona; a política de CI impede sua remoção. O monitor externo do GitHub permanece como segunda perspectiva independente e somente leitura. Uma publicação manual normal exige os quatro alvos saudáveis; o modo de recuperação precisa ser escolhido explicitamente, ainda exige staging saudável e valida health e shell de produção depois da correção, evitando que uma falha preexistente impeça o próprio reparo. | Worker/Cron/KV/Analytics Engine isolados e GitHub Actions | aprovada |
| WFL-01 | Workflows | Criar e editar workflow | `/workflows` | fora do escopo |
| WFL-02 | Workflows | Executar, reprocessar e auditar workflow | `/workflows/:id` | fora do escopo |
| COEX-01 | Coexistência | Onboarding e operação em coexistência Meta | integração Meta | fora do escopo |

## Lacunas abertas conhecidas

| Origem | Jornada afetada | Lacuna | Próxima evidência necessária |
| --- | --- | --- | --- |
| Integração externa | MINI-07 | Google Calendar exige configuração e consentimento OAuth reais | Configurar uma instalação e executar autorização, slots e criação de evento |
| Delta oficial Meta 16/07/2026 | META-01 | Contratos e persistência BSUID passaram localmente, mas a conta de teste ainda não produziu evento real sem telefone/`recipient_id` | Receber um evento real de usuário com username/BSUID e confirmar resposta e status no provedor |
| Varredura original × migrado 18/07/2026 | NAV-01 | Corrigido: ajuda abre atalhos reais, alertas usam a saúde operacional e não exibem badge fixo, e o botão de desenvolvedor sem consumidor foi removido | Manter regressão de teclado, tema, alertas e rotas inválidas |
| Varredura original × migrado 18/07/2026 | ONB-01 | O original possui overlay, checklist, modal de credenciais, retomada e tour; a casca migrada não contém essa jornada | Definir o onboarding exigido para instalação por cliente, implementar e executar primeira instalação/retomada |
| Varredura original × migrado 18/07/2026 | PWA-01 | Provider, manifesto, service worker e Push presentes no original não têm equivalentes no migrado | Decidir se PWA permanece no produto; se sim, implementar e testar instalação, update e permissões; se não, marcar descontinuada |
| Varredura original × migrado 18/07/2026 | SEC-02 | Workflows continuam com API CRUD/execução montada embora fora do escopo; memória/histórico de contatos permanecem em endpoints, hooks e componente inacessível apesar de descontinuados | Remover ou bloquear contratos e código morto; testar 404/410/403 e regressão das jornadas mantidas |
| Reteste real WAHooks 19/07/2026 | WHK-01 | Corrigido e aprovado para o escopo de teste: a conexão WAHooks “Suporte” foi usada somente como número de teste; a mensagem entrou pelo webhook oficial Meta, acionou IA/RAG e voltou pela API oficial. Ingestão direta por callback WAHooks permanece fora do escopo. | Se o escopo ampliar para callback WAHooks direto, definir endpoint assinado, allowlist, idempotência, correlação e limite de turnos |
| Simulação multi-turno WAHooks 19/07/2026 | WHK-02 | Em teste: três turnos explícitos receberam resposta da IA; uma formulação ambígua acionou handoff para humano; após “Devolver à IA”, o turno seguinte respondeu normalmente. A continuidade automática precisa de uma decisão de produto para perguntas sem fonte: handoff imediato ou tentativa de esclarecimento. | Definir a política de ambiguidade e repetir a conversa multi-turno com essa política implementada e observada no Inbox e no canal WAHooks |
| Simulação comercial real 19/07/2026 | AI-05 / WHK-03 | Em teste: prompt e FAQ reais do SmartZap foram configurados e indexados; a conversa respondeu sobre API, consentimento, campanhas e webhooks. Antes da correção, repetiu uma pergunta já respondida; após a correção do foco na última mensagem, respondeu corretamente e transferiu preço/prazo para humano. | Repetir a jornada limpa com mais um ciclo de qualificação e confirmar avanço sem repetição, com handoff comercial observável |
